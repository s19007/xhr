import React from 'react'

const  Greeting = props => (
  h1{props.type})
class App extends React.Component {
    constructor (props) {
      super(props)
      this.state = { data: []}
    }
    render () {
      return ()}
    componentDidMount () {
      fetch('')
        .then(res => res.json())
        .then(console.log)
        .then
    }

    render () {
      return (
export default App;
